# Web server
PORT=3000
APP_URL=http://localhost:3000

# MySQL
DB_HOST=localhost
DB_USER=replace_with_database_user
DB_PASSWORD=replace_with_database_password
DB_NAME=campus_marketplace

# Outgoing verification email (SMTP)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=replace_with_email_address
EMAIL_PASSWORD=replace_with_email_password

# Generate a long, random value for each deployed environment.
EMAIL_VERIFICATION_SECRET=replace_with_a_long_random_secret
